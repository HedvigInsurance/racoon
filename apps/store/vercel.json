{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && yarn turbo run codegen --filter=store --force && yarn turbo run build --filter=store",
  "ignoreCommand": "../../bin/vercel-ignore-step.sh",
  "regions": ["arn1"],
  "github": {
    "autoJobCancelation": false
  }
}