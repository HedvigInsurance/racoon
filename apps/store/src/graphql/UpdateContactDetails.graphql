mutation ContactDetailsUpdate($input: CheckoutContactDetailsUpdateInput) {
  checkoutContactDetailsUpdate(input: $input) {
    checkout {
      ...Checkout
    }
    userError {
      message
    }
  }
}
