mutation CartLinesAdd($cartId: ID!, $lineItemId: ID!) {
  cartLinesAdd(input: { cartId: $cartId, lineItemIds: [$lineItemId] }) {
    cart {
      ...CartFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
