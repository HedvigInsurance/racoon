mutation ExternalInsurerUpdate($priceIntentId: ID!, $externalInsurerId: String) {
  priceIntentExternalInsurerUpdate(
    priceIntentId: $priceIntentId
    externalInsurerId: $externalInsurerId
  ) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
