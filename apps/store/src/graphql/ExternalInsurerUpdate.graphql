mutation ExternalInsurerUpdate($priceIntentId: UUID!, $externalInsurerId: String) {
  priceIntentExternalInsurerUpdate(
    priceIntentId: $priceIntentId
    externalInsurerId: $externalInsurerId
  ) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
