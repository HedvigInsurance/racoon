query ShopSessionRetargeting($shopSessionId: UUID!) {
  shopSession(id: $shopSessionId) {
    id
    cart {
      entries {
        id
      }
    }
    priceIntents {
      ...RetargetingPriceIntent
    }
  }
}
