mutation CartEntryRemove($cartId: ID!, $offerId: ID!) {
  cartEntriesRemove(input: { cartId: $cartId, offerIds: [$offerId] }) {
    cart {
      ...CartFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
