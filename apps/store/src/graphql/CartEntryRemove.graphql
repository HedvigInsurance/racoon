mutation CartEntryRemove($cartId: ID!, $offerId: ID!, $locale: String!) {
  cartEntriesRemove(input: { cartId: $cartId, offerIds: [$offerId] }) {
    cart {
      ...CartFragment
    }
    userError {
      message
    }
  }
}
