mutation SwitchConfirmation($switcherCaseCompleteId: UUID!, $currentExpiryDate: Date!) {
  switcherCaseComplete(id: $switcherCaseCompleteId, currentExpiryDate: $currentExpiryDate) {
    message
  }
}
