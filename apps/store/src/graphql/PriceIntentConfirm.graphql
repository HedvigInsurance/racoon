mutation PriceIntentConfirm($priceIntentId: UUID!) {
  priceIntentConfirm(priceIntentId: $priceIntentId) {
    priceIntent {
      ...PriceIntent
      defaultOffer {
        id
      }
    }
    userError {
      message
    }
  }
}
