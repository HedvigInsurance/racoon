mutation PriceIntentConfirm($priceIntentId: ID!, $locale: String!) {
  priceIntentConfirm(priceIntentId: $priceIntentId) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
