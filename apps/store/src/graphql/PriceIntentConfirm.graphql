mutation PriceIntentConfirm($priceIntentId: ID!) {
  priceIntentConfirm(priceIntentId: $priceIntentId) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
