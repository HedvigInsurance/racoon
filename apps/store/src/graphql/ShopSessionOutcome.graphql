query ShopSessionOutcome($id: UUID!) {
  shopSession(id: $id) {
    outcome {
      id
      createdContracts {
        id
        variant {
          displayName
        }
        externalInsuranceCancellation {
          ...ContractExternalInsuranceCancellation
        }
      }
    }
  }
}
