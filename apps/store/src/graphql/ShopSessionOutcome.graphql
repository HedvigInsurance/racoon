query ShopSessionOutcome($shopSessionId: UUID!) {
  shopSession(id: $shopSessionId) {
    id
    outcome {
      id
      createdContracts {
        id
        variant {
          displayName
        }
        externalInsuranceCancellation {
          ...ContractExternalInsuranceCancellation
        }
      }
    }
  }
}
