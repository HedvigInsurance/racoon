mutation StartDateUpdate($priceIntentId: ID!, $startDate: Date) {
  priceIntentStartDateUpdate(priceIntentId: $priceIntentId, startDate: $startDate) {
    priceIntent {
      ...PriceIntentFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
