mutation StartDateUpdate($priceIntentId: ID!, $startDate: Date!, $locale: String!) {
  priceIntentStartDateUpdate(priceIntentId: $priceIntentId, startDate: $startDate) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
