mutation StartDateUpdate($lineItemId: ID!, $startDate: Date) {
  cartLineStartDateUpdate(input: { lineItemId: $lineItemId, startDate: $startDate }) {
    cart {
      ...CartFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
