mutation StartDateUpdate($productOfferIds: [ID!]!, $startDate: Date!) {
  productOffersStartDateUpdate(productOfferIds: $productOfferIds, startDate: $startDate) {
    productOffers {
      ...ProductOffer
    }
    userError {
      message
    }
  }
}
