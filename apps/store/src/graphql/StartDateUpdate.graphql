mutation StartDateUpdate($productOfferIds: [ID!]!, $startDate: Date!, $locale: String!) {
  productOffersStartDateUpdate(productOfferIds: $productOfferIds, startDate: $startDate) {
    productOffers {
      ...ProductOffer
    }
    userError {
      message
    }
  }
}
