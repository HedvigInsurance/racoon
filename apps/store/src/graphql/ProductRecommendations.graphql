query ProductRecommendations($shopSessionId: UUID!) {
  shopSession(id: $shopSessionId) {
    recommendations {
      product {
        ...ProductRecommendation
      }
      offer {
        ...OfferRecommendation
      }
    }
  }
}
