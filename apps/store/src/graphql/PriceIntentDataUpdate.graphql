mutation PriceIntentDataUpdate($priceIntentId: ID!, $data: PricingFormData!, $locale: String!) {
  priceIntentDataUpdate(priceIntentId: $priceIntentId, data: $data) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
