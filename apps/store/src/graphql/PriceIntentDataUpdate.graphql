mutation PriceIntentDataUpdate($priceIntentId: ID!, $data: JSON!, $locale: String!) {
  priceIntentDataUpdate(priceIntentId: $priceIntentId, data: $data) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
