mutation PriceIntentDataUpdate($priceIntentId: ID!, $data: PricingFormData!) {
  priceIntentDataUpdate(priceIntentId: $priceIntentId, data: $data) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
