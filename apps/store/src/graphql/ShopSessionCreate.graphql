mutation ShopSessionCreate($countryCode: CountryCode!) {
  shopSessionCreate(input: { countryCode: $countryCode }) {
    id
    countryCode
    currencyCode
    cart {
      ...CartFragment
    }
  }
}
