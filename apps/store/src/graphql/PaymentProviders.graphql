query PaymentProviders($countryCode: CountryCode!) {
  paymentProviders(countryCode: $countryCode) {
    ... on PaymentProviderTrustly {
      _
    }
    ... on PaymentProviderAdyen {
      availablePaymentMethods
    }
  }
}
