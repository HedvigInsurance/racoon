fragment CartFragment on Cart {
  id
  cost {
    subtotal {
      amount
      currencyCode
    }
    total {
      amount
      currencyCode
    }
  }
  entries {
    id
    startDate
    price {
      amount
      currencyCode
    }
    variant {
      id
      title
    }
  }
}
