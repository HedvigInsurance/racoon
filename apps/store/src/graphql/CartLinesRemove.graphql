mutation CartLinesRemove($cartId: ID!, $lineItemId: ID!) {
  cartLinesRemove(input: {cartId: $cartId, lineItemIds: [$lineItemId]}) {
    cart {
      ...CartFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
