mutation PriceIntentInsurelyUpdate($priceIntentId: ID!, $dataCollectionId: String!) {
  priceIntentInsurelyUpdate(priceIntentId: $priceIntentId, dataCollectionId: $dataCollectionId) {
    priceIntent {
      ...PriceIntentFragment
    }
    userError {
      message
    }
  }
}
