mutation CheckoutStartSign($checkoutId: ID!) {
  checkoutStartSign(checkoutId: $checkoutId) {
    signing {
      ...CheckoutSigning
    }
    userErrors {
      code
      field
      message
    }
  }
}
