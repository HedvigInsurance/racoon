mutation RedeemCampaign($cartId: ID!, $code: String!, $locale: String!) {
  cartRedeemCampaign(input: { cartId: $cartId, code: $code }) {
    cart {
      ...CartFragment
    }
    userError {
      message
    }
  }
}
