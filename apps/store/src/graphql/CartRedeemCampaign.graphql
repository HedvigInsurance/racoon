mutation RedeemCampaign($cartId: ID!, $code: String!) {
  cartRedeemCampaign(input: { cartId: $cartId, code: $code }) {
    cart {
      ...CartFragment
    }
    userErrors {
      code
      field
      message
    }
  }
}
