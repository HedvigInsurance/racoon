fragment PriceIntentFragment on PriceIntent {
  id
  data
  suggestedData
  offers {
    ...ProductOffer
  }
  startDate
  cancellation {
    externalInsurer {
      id
      displayName
    }
    option
    requested
  }
}
