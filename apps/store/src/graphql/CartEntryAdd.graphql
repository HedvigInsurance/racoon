mutation CartEntryAdd($cartId: ID!, $offerId: ID!) {
  cartEntriesAdd(input: { cartId: $cartId, offerIds: [$offerId] }) {
    cart {
      ...CartFragment
    }
    userError {
      message
    }
  }
}
