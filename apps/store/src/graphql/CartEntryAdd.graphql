mutation CartEntryAdd($cartId: ID!, $offerId: ID!, $locale: String!) {
  cartEntriesAdd(input: { cartId: $cartId, offerIds: [$offerId] }) {
    cart {
      ...CartFragment
    }
    userError {
      message
    }
  }
}
