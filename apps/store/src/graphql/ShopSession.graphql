query ShopSession($shopSessionId: ID!) {
  shopSession(id: $shopSessionId) {
    id
    countryCode
    currencyCode
    cart {
      ...CartFragment
    }
    checkout {
      ...Checkout
    }
  }
}
