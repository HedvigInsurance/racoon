query CarTrialExtension($contractId: UUID!) {
  carTrial(contractId: $contractId) {
    id
    priceIntent {
      ...PriceIntentCarTrialExtension
    }
    shopSession {
      ...ShopSession
    }
    trialContract {
      ...TrialContract
    }
  }
}
