{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && yarn turbo run codegen --filter=onboarding --force --no-cache && yarn turbo run build --filter=onboarding --force --no-cache",
  "ignoreCommand": "../../bin/vercel-ignore-step.sh",
  "installCommand": "cd ../.. && yarn install --immutable",
  "regions": ["arn1"],
  "github": {
    "autoJobCancelation": true
  }
}
