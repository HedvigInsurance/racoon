mutation refundDeptToMember(
  $memberId: ID!
  $initiateAutomaticPayout: Boolean!
) {
  refundDeptToMember(
    memberId: $memberId
    initiateAutomaticPayout: $initiateAutomaticPayout
  ) {
    memberId
  }
}
